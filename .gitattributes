# Paper figures and small tracked checkpoints via LFS (opt-in; harmless if LFS absent).
analysis/figures/*.pdf filter=lfs diff=lfs merge=lfs -text
analysis/figures/*.png filter=lfs diff=lfs merge=lfs -text
paper/figures/*.pdf    filter=lfs diff=lfs merge=lfs -text

# Keep the ledger as a normal text diff so results are reviewable.
results/ledger.jsonl -text
