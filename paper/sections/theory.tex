\section{Guarantees}
% State the selective-risk guarantee (LTT): P(R_sel(lambda_hat) <= alpha) >= 1 - delta under
% exchangeability. State the halting guarantee (CRC): E[L(lambda_hat)] <= alpha. Make the
% exchangeability assumption explicit and note the marginal-over-calibration caveat.
\begin{assumption}[Exchangeability]
The calibration and test inputs are exchangeable draws from the deployment distribution.
\end{assumption}
