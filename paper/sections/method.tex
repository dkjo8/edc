\section{Method}
% (a) K-restart Langevin descent over the latent. (b) Geometry features: basin agreement,
% terminal-energy stats, curvature via HVP (power iteration lambda_max, Hutchinson trace),
% descent dynamics. (c) Nonconformity mapper fit on a fold disjoint from calibration.
% (d) CRC for halting (monotone risk); LTT for selective error (non-monotone risk).
